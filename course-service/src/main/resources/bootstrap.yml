spring:
    application:
        name: course-service
    cloud:
        config:
            server:
                git:
                    uri: S:\Git\jcoursescloud\config-repo
    profiles:
        active: dev
