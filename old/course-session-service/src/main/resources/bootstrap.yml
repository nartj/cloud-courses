spring:
    application:
        name: course-session-service
    cloud:
        config:
            server:
                git:
                    uri: S:\Git\jcoursescloud\config-repo
    profiles:
        active: dev
